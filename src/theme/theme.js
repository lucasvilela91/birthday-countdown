import { createTheme } from '@mui/material/styles';
import '@fontsource/boogaloo'; // Importa a fonte Boogaloo

const theme = createTheme({
  typography: {
    fontFamily: 'Boogaloo, Arial, sans-serif',
  },
});

export default theme;
